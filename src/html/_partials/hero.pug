- var heroTitle = pageTitle ? pageTitle : siteName
- var heroTagline = pageTagline ? pageTagline : siteTagline
section
  article.hero
    h1 #{heroTitle}
    if heroTagline
      each tagline in heroTagline
        p !{tagline}
    if !formId && !noCta
      a.link(href="/apply.html") Join the program
