block variables
- var siteName = locals.site && locals.site.siteName ? locals.site.siteName : ''
- var siteDesc = locals.site && locals.site.siteDescription ? locals.site.siteDescription : ''
- var siteAuthor = locals.site && locals.site.siteAuthor ? locals.site.siteAuthor : {}
- var siteTagline = locals.site && locals.site.siteTagline ? locals.site.siteTagline : []
- var nav = locals.site && locals.site.nav ? locals.site.nav : []
- var resources = locals.resources ? locals.resources : []
- var daily = locals.daily ? locals.daily : []
- var tips = locals.tips ? locals.tips : []
- var date = new Date()
- var year = date.getFullYear()

- function formatNumber(num)
  - return num < 10 ? '0' + num : num;

- function formatDate(date)
  - var d = new Date(date);
  - return d.getFullYear() + '-' + formatNumber(d.getMonth() + 1) + '-' + formatNumber(d.getDate());
