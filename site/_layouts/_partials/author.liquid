<section class="wrapper wrapper--alpha">
  <article class="author">
    <h2>Your Mentor</h2>
    {% if site.author.about %}
      {% for val in site.author.about %}
        <p>{{ val }}</p>
      {% endfor %}
    {% endif %}
    {% include _partials/profile %}
  </article>
</section>
