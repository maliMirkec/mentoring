<section>
  <article class="intro">
    <h2>Tips</h2>
    <p>The tips I gathered throughout the years have two primary purposes:</p>
    <ul>
      <li>To inspire you, and</li>
      <li>To help you become a better UI developer.</li>
    </ul>
    <p>The tips are also available as <a href="/slides.html" class="href">slides</a>.</p>
  </article>
  <div class="row">
    {% for tip in tips %}
      {% if forloop.index < 2 %}
        <div class="col">
          <div class="card wrapper wrapper--beta">
            <h3>{{ tip.name }}</h3>
            <ul>
              {% for advice in tip.list %}
                {% if forloop.index < 4 %}
                  <li>{{ advice.title }}</li>
                {% endif %}
              {% endfor %}
            </ul>
            <a href="/tips.html" class="href">See all tips</a>
          </div>
        </div>
      {% endif %}
    {% endfor %}
  </div>
</section>
